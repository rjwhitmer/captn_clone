<% if @teams.any? %>
  <h1>Current Teams</h1>
  <% @teams.each do |team| %>
  <div>
    <%= link_to team.name, team_path(team.id) %>
    <%= team.team_color %>
  </div>
  <% end %>
<% else %>
  <h3>No teams yet!</h3>
<% end %>
