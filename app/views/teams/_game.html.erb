<% @games.each do |game| %>
  <%= link_to team_game_path(@team, game) do %>
    <div>
      <%= game.sport %>
      <%= game.game_date_time.to_date unless game.game_date_time.blank? %>
      <%= game.location %>
    </div>
  <% end %>
<% end %>
